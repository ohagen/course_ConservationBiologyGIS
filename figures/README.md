# Figures

This directory holds map exports and other figures produced by the R spatial
tutorial (`r_spatial_tutorial.qmd`).

Figures are generated at render time and are not tracked in version control.
